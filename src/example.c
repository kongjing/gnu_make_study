#include<add.h>
#include<sub.h>
#include<need.h>
#include<stdio.h>

int main()
{
    printf("%d, %d\n", add(100, 80), sub(100, 80));
    printf("the DEBUG is =%d\n", DEBUG); 
    printf("the subbug is =%d\n", subbug); 
    printf("the NEED is =%d\n", NEED); 
    return 0;
}
	
