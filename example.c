#include<add.h>
#include<sub.h>
#include<stdio.h>

int main()
{
    printf("%d, %d\n", add(100, 50), sub(100,50));
    printf("the DEBUG is =%d\n", DEBUG); 
    printf("the subbug is =%d\n", subbug); 
    return 0;
}
	
